import { vowelsMode } from "./vowels";
import { maFamilyMode } from "./maFamily";
import { paFamilyMode } from "./paFamily";

export const gameModes = {
  vowels: vowelsMode,
  maFamily: maFamilyMode,
  paFamily: paFamilyMode,
};
